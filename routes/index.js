module.exports = (app, controllers) => {
  // var userRoutes = require('./user')(controllers.userController)
  // app.use('/user/', userRoutes)

  // var taskRoutes = require('./task')(controllers.taskController)
  // app.use('/task/', taskRoutes)

  // var tagRoutes = require('./tag')(controllers.tagController)
  // app.use('/tag/', tagRoutes)

}
